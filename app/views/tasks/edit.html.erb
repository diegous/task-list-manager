<h2>Edit <%= @task.type.titleize %></h2>

<%= simple_form_for([@task.list, @task.becomes(Task)], url: list_task_path(@task.list, @task)) do |f| %>
  <%= render 'edit_form', form: f, task: @task, type: @task.type show_state_select: true %>
<% end %>

<%= link_to 'Back', lists_path, class: 'btn btn-default' %>
