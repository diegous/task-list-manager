<h2>New <%= @type.titleize %></h2>

<%= simple_form_for([@list, @task], url: list_tasks_path) do |f| %>
  <%= f.hidden_field :type, value: @type %>
  <%= render 'edit_form', form: f, task: @task, type: @type, show_state_select: false %>
<% end %>

<%= link_to 'Back', lists_path %>
